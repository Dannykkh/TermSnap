<Window x:Class="TermSnap.Views.SnippetManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{DynamicResource SnippetManager.Title}"
        Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <TextBlock Grid.Row="0"
                   Text="{DynamicResource SnippetManager.Title}"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- 검색 및 필터 -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchTextBox"
                     Grid.Column="0"
                     materialDesign:HintAssist.Hint="{DynamicResource SnippetManager.SearchHint}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     TextChanged="SearchTextBox_TextChanged"/>

            <ComboBox x:Name="CategoryComboBox"
                      Grid.Column="1"
                      Width="150"
                      Margin="10,0,0,0"
                      materialDesign:HintAssist.Hint="{DynamicResource SnippetManager.CategoryHint}"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      SelectionChanged="CategoryComboBox_SelectionChanged"/>

            <Button Grid.Column="2"
                    Content="{DynamicResource SnippetManager.NewSnippet}"
                    Margin="10,0,0,0"
                    Click="NewSnippet_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </Grid>

        <!-- 스니펫 목록 -->
        <DataGrid x:Name="SnippetsDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  IsReadOnly="True"
                  MouseDoubleClick="SnippetsDataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource SnippetManager.Name}"
                                    Binding="{Binding Name}"
                                    Width="150"/>
                <DataGridTextColumn Header="{DynamicResource SnippetManager.Description}"
                                    Binding="{Binding Description}"
                                    Width="200"/>
                <DataGridTextColumn Header="{DynamicResource SnippetManager.Command}"
                                    Binding="{Binding Command}"
                                    Width="*"/>
                <DataGridTextColumn Header="{DynamicResource SnippetManager.CategoryHint}"
                                    Binding="{Binding Category}"
                                    Width="100"/>
                <DataGridTextColumn Header="{DynamicResource SnippetManager.UseCount}"
                                    Binding="{Binding UseCount}"
                                    Width="80"/>
                <DataGridTemplateColumn Header="{DynamicResource SnippetManager.Actions}" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="{DynamicResource Common.Edit}"
                                        Click="EditSnippet_Click"
                                        Margin="0,0,5,0"
                                        Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Height="28"/>
                                <Button Content="{DynamicResource Common.Delete}"
                                        Click="DeleteSnippet_Click"
                                        Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Height="28"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 버튼 -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <TextBlock Text="{DynamicResource SnippetManager.TotalCount}"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>
            <TextBlock x:Name="CountTextBlock"
                       Text="0"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="0,0,20,0"/>
            <Button Content="{DynamicResource Common.Close}"
                    Click="Close_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
