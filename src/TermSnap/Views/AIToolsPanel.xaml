<UserControl x:Class="TermSnap.Views.AIToolsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource SurfaceBrush}"
             Focusable="False"
             MinWidth="400"
             PreviewMouseWheel="UserControl_PreviewMouseWheel">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- claude-mem 스타일 색상 (카테고리별) -->
        <SolidColorBrush x:Key="MemoryArchitectureBrush" Color="#0969da"/>    <!-- 파란색 - 아키텍처 -->
        <SolidColorBrush x:Key="MemoryArchitectureBgBrush" Color="#f0f6fb"/>
        <SolidColorBrush x:Key="MemoryPatternBrush" Color="#8250df"/>         <!-- 보라색 - 패턴 -->
        <SolidColorBrush x:Key="MemoryPatternBgBrush" Color="#f5f0ff"/>
        <SolidColorBrush x:Key="MemoryToolBrush" Color="#1a7f37"/>            <!-- 초록색 - 도구 -->
        <SolidColorBrush x:Key="MemoryToolBgBrush" Color="#f0fff4"/>
        <SolidColorBrush x:Key="MemoryGotchaBrush" Color="#cf222e"/>          <!-- 빨간색 - 주의사항 -->
        <SolidColorBrush x:Key="MemoryGotchaBgBrush" Color="#fff5f5"/>
        <SolidColorBrush x:Key="MemoryGoalBrush" Color="#d4a72c"/>            <!-- 금색 - 목표 -->
        <SolidColorBrush x:Key="MemoryGoalBgBrush" Color="#fffbf0"/>
        <SolidColorBrush x:Key="MemoryMetaBrush" Color="#6e7781"/>            <!-- 회색 - 메타 -->
        <SolidColorBrush x:Key="MemoryMetaBgBrush" Color="#f6f8fa"/>
        <SolidColorBrush x:Key="ConversationBrush" Color="#0969da"/>          <!-- 파란색 - 대화 -->
        <SolidColorBrush x:Key="ConversationBgBrush" Color="#f0f6fb"/>

        <!-- 탭 버튼 스타일 -->
        <Style x:Key="TabButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource CardBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border CornerRadius="8"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            Background="{DynamicResource SurfaceBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 헤더 -->
            <Border Grid.Row="0"
                    Background="{DynamicResource PrimaryBrush}"
                    CornerRadius="8,8,0,0"
                    Padding="12,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Robot"
                                                 Width="20" Height="20"
                                                 VerticalAlignment="Center"
                                                 Foreground="White"/>
                        <TextBlock Text="AI Tools"
                                   FontWeight="Bold"
                                   FontSize="14"
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0"/>
                    </StackPanel>

                    <!-- Claude Hook 설정 버튼 -->
                    <Button x:Name="SetupHooksButton"
                            Grid.Column="2"
                            Style="{StaticResource MaterialDesignIconButton}"
                            Width="28" Height="28"
                            Click="SetupHooksButton_Click"
                            ToolTip="Claude 장기기억 훅 설정 (MEMORY.md, CLAUDE.md 생성)">
                        <materialDesign:PackIcon Kind="Cog" Width="16" Height="16" Foreground="White"/>
                    </Button>

                    <!-- 닫기 버튼 -->
                    <Button x:Name="CloseButton"
                            Grid.Column="3"
                            Style="{StaticResource MaterialDesignIconButton}"
                            Width="28" Height="28"
                            Click="CloseButton_Click"
                            ToolTip="닫기">
                        <materialDesign:PackIcon Kind="Close" Width="16" Height="16" Foreground="White"/>
                    </Button>
                </Grid>
            </Border>

            <!-- 탭 헤더 -->
            <Border Grid.Row="1" Background="{DynamicResource BackgroundBrush}" Padding="8,6">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <RadioButton x:Name="MemoryTabButton"
                                 IsChecked="True"
                                 Checked="TabButton_Checked"
                                 Tag="Memory"
                                 Style="{StaticResource TabButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Brain" Width="16" Height="16" VerticalAlignment="Center"/>
                            <TextBlock Text="Memory" Margin="6,0,0,0" VerticalAlignment="Center" FontSize="12"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="OrchestratorTabButton"
                                 Checked="TabButton_Checked"
                                 Tag="Orchestrator"
                                 Style="{StaticResource TabButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="AccountGroup" Width="16" Height="16" VerticalAlignment="Center"/>
                            <TextBlock Text="Orchestrator" Margin="6,0,0,0" VerticalAlignment="Center" FontSize="12"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="SkillsTabButton"
                                 Checked="TabButton_Checked"
                                 Tag="Skills"
                                 Style="{StaticResource TabButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Puzzle" Width="16" Height="16" VerticalAlignment="Center"/>
                            <TextBlock Text="Skills" Margin="6,0,0,0" VerticalAlignment="Center" FontSize="12"/>
                        </StackPanel>
                    </RadioButton>
                </StackPanel>
            </Border>

            <!-- 탭 컨텐츠 -->
            <Grid Grid.Row="2">
                <!-- Memory 컨텐츠 -->
                <Grid x:Name="MemoryContent" Visibility="Visible">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 서브탭 버튼 -->
                    <Border Grid.Row="0" Background="{DynamicResource CardBrush}"
                            Padding="8,4" Margin="8,8,8,0" CornerRadius="6">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <RadioButton x:Name="MemorySubTabMemories"
                                         IsChecked="True"
                                         Checked="MemorySubTab_Checked"
                                         Tag="Memories"
                                         Style="{StaticResource TabButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Brain" Width="14" Height="14" VerticalAlignment="Center"/>
                                    <TextBlock Text="기억" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="11"/>
                                </StackPanel>
                            </RadioButton>
                            <RadioButton x:Name="MemorySubTabConversations"
                                         Checked="MemorySubTab_Checked"
                                         Tag="Conversations"
                                         Style="{StaticResource TabButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Chat" Width="14" Height="14" VerticalAlignment="Center"/>
                                    <TextBlock Text="대화" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="11"/>
                                </StackPanel>
                            </RadioButton>
                            <RadioButton x:Name="MemorySubTabSearch"
                                         Checked="MemorySubTab_Checked"
                                         Tag="Search"
                                         Style="{StaticResource TabButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Magnify" Width="14" Height="14" VerticalAlignment="Center"/>
                                    <TextBlock Text="검색" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="11"/>
                                </StackPanel>
                            </RadioButton>
                        </StackPanel>
                    </Border>

                    <!-- 서브탭 컨텐츠 영역 -->
                    <Grid Grid.Row="1">
                        <!-- 기억 서브탭 -->
                        <Grid x:Name="MemoriesSubContent" Visibility="Visible">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- 타입 필터 -->
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="12,8,12,0">
                                <TextBlock Text="타입:" VerticalAlignment="Center" Margin="0,0,8,0"
                                           Foreground="{DynamicResource TextPrimaryBrush}" FontSize="12"/>
                                <ComboBox x:Name="MemoryTypeFilterCombo" Width="140"
                                          SelectionChanged="MemoryTypeFilterCombo_SelectionChanged"
                                          Foreground="{DynamicResource TextPrimaryBrush}"
                                          Background="{DynamicResource CardBrush}"
                                          FontSize="11">
                                    <ComboBoxItem Content="전체" Tag="" IsSelected="True"/>
                                    <ComboBoxItem Content="🏗️ 아키텍처" Tag="Architecture"/>
                                    <ComboBoxItem Content="🔄 패턴" Tag="Pattern"/>
                                    <ComboBoxItem Content="🔧 도구" Tag="Tool"/>
                                    <ComboBoxItem Content="⚠️ 주의사항" Tag="Gotcha"/>
                                    <ComboBoxItem Content="🎯 목표" Tag="Goal"/>
                                    <ComboBoxItem Content="📁 메타" Tag="Meta"/>
                                </ComboBox>

                                <Button Style="{StaticResource MaterialDesignFlatButton}"
                                        Margin="8,0,0,0" Click="MemoryRefreshButton_Click"
                                        ToolTip="새로고침" Width="32" Height="32">
                                    <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16"/>
                                </Button>
                            </StackPanel>

                            <!-- 메모리 목록 (스크롤 가능) -->
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                                          Margin="8,8,8,0"
                                          PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                                <ListBox x:Name="MemoryList"
                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                         ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                         SelectionChanged="MemoryList_SelectionChanged"
                                         MouseDoubleClick="MemoryList_MouseDoubleClick"
                                         Background="Transparent"
                                         BorderThickness="0">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="Margin" Value="0"/>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <!-- claude-mem 스타일 카드: 왼쪽 색상 바 + 카드 -->
                                            <Border Margin="0,4" CornerRadius="6"
                                                    Background="{DynamicResource CardBrush}"
                                                    BorderBrush="{Binding TypeBorderBrush}"
                                                    BorderThickness="0,0,0,0">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="4"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- 왼쪽 색상 바 (타입별) -->
                                                    <Border Grid.Column="0"
                                                            Background="{Binding TypeBorderBrush}"
                                                            CornerRadius="6,0,0,6"/>

                                                    <!-- 카드 내용 -->
                                                    <Border Grid.Column="1" Padding="12,10"
                                                            Background="{DynamicResource CardBrush}"
                                                            CornerRadius="0,6,6,0">
                                                        <Grid>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                            </Grid.RowDefinitions>

                                                            <!-- 헤더: 아이콘 + 타입 뱃지 -->
                                                            <Grid Grid.Row="0" Margin="0,0,0,6">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>

                                                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                                    <TextBlock Text="{Binding TypeIcon}" FontSize="16"
                                                                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                                    <Border Background="{Binding TypeBorderBrush}"
                                                                            CornerRadius="3" Padding="6,2">
                                                                        <TextBlock Text="{Binding TypeName}"
                                                                                   FontSize="10" FontWeight="SemiBold"
                                                                                   Foreground="White"/>
                                                                    </Border>
                                                                </StackPanel>

                                                                <TextBlock Grid.Column="1"
                                                                           Text="{Binding CreatedAtDisplay}"
                                                                           FontSize="10"
                                                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                                                           HorizontalAlignment="Right"
                                                                           VerticalAlignment="Center"/>

                                                                <Button Grid.Column="2"
                                                                        Style="{StaticResource MaterialDesignIconForegroundButton}"
                                                                        Width="20" Height="20"
                                                                        Margin="8,0,0,0"
                                                                        Click="DeleteMemory_Click"
                                                                        Tag="{Binding Id}"
                                                                        ToolTip="삭제">
                                                                    <materialDesign:PackIcon Kind="Close" Width="12" Height="12"/>
                                                                </Button>
                                                            </Grid>

                                                            <!-- 내용 -->
                                                            <TextBlock Grid.Row="1"
                                                                       Text="{Binding Content}"
                                                                       TextWrapping="Wrap"
                                                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                                                       FontSize="12"
                                                                       LineHeight="18"/>

                                                            <!-- 태그 -->
                                                            <StackPanel Grid.Row="2" Orientation="Horizontal"
                                                                        Margin="0,8,0,0"
                                                                        Visibility="{Binding HasContext, Converter={StaticResource BoolToVis}}">
                                                                <Border Background="{DynamicResource BackgroundBrush}"
                                                                        CornerRadius="3" Padding="6,2">
                                                                    <TextBlock Text="{Binding Context}"
                                                                               FontSize="10"
                                                                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                                </Border>
                                                            </StackPanel>
                                                        </Grid>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </ScrollViewer>
                        </Grid>

                        <!-- 대화 서브탭 -->
                        <Grid x:Name="ConversationsSubContent" Visibility="Collapsed">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- 대화 목록 헤더 -->
                            <Grid Grid.Row="0" Margin="12,8,12,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="📅 최근 대화 기록" FontWeight="Medium" FontSize="12"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           VerticalAlignment="Center"/>
                                <Button Grid.Column="1"
                                        Style="{StaticResource MaterialDesignFlatButton}"
                                        Click="ConversationRefreshButton_Click"
                                        ToolTip="새로고침" Width="32" Height="32">
                                    <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16"/>
                                </Button>
                            </Grid>

                            <!-- 대화 목록 (스크롤 가능) -->
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                                          Margin="8,8,8,0"
                                          PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                                <ListBox x:Name="ConversationList"
                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                         ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                         SelectionChanged="ConversationList_SelectionChanged"
                                         Background="Transparent" BorderThickness="0">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="Margin" Value="0"/>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <!-- claude-mem 스타일 대화 카드 -->
                                            <Border Margin="0,4" CornerRadius="6"
                                                    Background="{DynamicResource CardBrush}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="4"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- 왼쪽 색상 바 (파란색) -->
                                                    <Border Grid.Column="0"
                                                            Background="{StaticResource ConversationBrush}"
                                                            CornerRadius="6,0,0,6"/>

                                                    <!-- 카드 내용 -->
                                                    <Border Grid.Column="1" Padding="12,10"
                                                            Background="{DynamicResource CardBrush}"
                                                            CornerRadius="0,6,6,0">
                                                        <Grid>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                            </Grid.RowDefinitions>

                                                            <!-- 헤더 -->
                                                            <Grid Grid.Row="0" Margin="0,0,0,4">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>

                                                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                                    <materialDesign:PackIcon Kind="MessageText"
                                                                                             Width="16" Height="16"
                                                                                             Foreground="{StaticResource ConversationBrush}"
                                                                                             VerticalAlignment="Center"
                                                                                             Margin="0,0,8,0"/>
                                                                    <TextBlock Text="{Binding DateDisplay}"
                                                                               FontWeight="SemiBold" FontSize="13"
                                                                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                                </StackPanel>

                                                                <Button Grid.Column="2"
                                                                        Style="{StaticResource MaterialDesignIconForegroundButton}"
                                                                        Width="24" Height="24"
                                                                        Click="OpenConversationFile_Click"
                                                                        Tag="{Binding FilePath}"
                                                                        ToolTip="파일 열기">
                                                                    <materialDesign:PackIcon Kind="OpenInNew" Width="14" Height="14"/>
                                                                </Button>
                                                            </Grid>

                                                            <!-- 요약 -->
                                                            <TextBlock Grid.Row="1"
                                                                       Text="{Binding Summary}"
                                                                       FontSize="11"
                                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                                       TextTrimming="CharacterEllipsis"
                                                                       Visibility="{Binding HasSummary, Converter={StaticResource BoolToVis}}"/>

                                                            <!-- 태그들 -->
                                                            <ItemsControl Grid.Row="2"
                                                                          ItemsSource="{Binding Keywords}"
                                                                          Margin="0,6,0,0"
                                                                          Visibility="{Binding HasKeywords, Converter={StaticResource BoolToVis}}">
                                                                <ItemsControl.ItemsPanel>
                                                                    <ItemsPanelTemplate>
                                                                        <WrapPanel Orientation="Horizontal"/>
                                                                    </ItemsPanelTemplate>
                                                                </ItemsControl.ItemsPanel>
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <Border Background="{DynamicResource BackgroundBrush}"
                                                                                CornerRadius="3" Padding="6,2"
                                                                                Margin="0,0,4,2">
                                                                            <TextBlock Text="{Binding}"
                                                                                       FontSize="9"
                                                                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                                        </Border>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>
                                                        </Grid>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </ScrollViewer>

                            <!-- 대화 미리보기 -->
                            <Border x:Name="ConversationPreviewBorder" Grid.Row="2"
                                    Visibility="Collapsed"
                                    Background="{DynamicResource CardBrush}"
                                    CornerRadius="6" Padding="10" Margin="8,8,8,0"
                                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                    MaxHeight="150">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock x:Name="ConversationPreviewTitle"
                                                   Text="미리보기" FontWeight="Bold" FontSize="11"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    </StackPanel>
                                    <ScrollViewer Grid.Row="1" MaxHeight="100"
                                                  VerticalScrollBarVisibility="Auto"
                                                  PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                                        <TextBlock x:Name="ConversationPreviewText"
                                                   Text="" TextWrapping="Wrap" FontSize="10"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </ScrollViewer>
                                </Grid>
                            </Border>

                            <!-- 대화 없음 메시지 -->
                            <TextBlock x:Name="NoConversationsText"
                                       Grid.Row="1"
                                       Text="대화 기록이 없습니다"
                                       Visibility="Collapsed"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="12"/>
                        </Grid>

                        <!-- 검색 서브탭 -->
                        <Grid x:Name="SearchSubContent" Visibility="Collapsed">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- 검색 입력 -->
                            <Grid Grid.Row="0" Margin="12,8,12,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBox x:Name="MemorySearchBox"
                                         materialDesign:HintAssist.Hint="기억 + 대화 통합 검색..."
                                         materialDesign:HintAssist.Foreground="{DynamicResource TextSecondaryBrush}"
                                         Background="{DynamicResource CardBrush}"
                                         Foreground="{DynamicResource TextPrimaryBrush}"
                                         BorderBrush="{DynamicResource BorderBrush}"
                                         CaretBrush="{DynamicResource TextPrimaryBrush}"
                                         Padding="10,8"
                                         Margin="0,0,8,0"
                                         FontSize="12"
                                         KeyDown="MemorySearchBox_KeyDown"/>

                                <Button Grid.Column="1"
                                        Style="{StaticResource MaterialDesignFlatButton}"
                                        Click="MemorySearchButton_Click"
                                        Foreground="{DynamicResource PrimaryBrush}"
                                        ToolTip="검색" Width="36" Height="36">
                                    <materialDesign:PackIcon Kind="Magnify" Width="18" Height="18"/>
                                </Button>
                            </Grid>

                            <!-- 검색 결과 (스크롤 가능) -->
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                                          Margin="8,8,8,0"
                                          PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                                <StackPanel>
                                    <!-- 검색 결과: 기억 -->
                                    <Border x:Name="SearchMemoryResultsBorder"
                                            Visibility="Collapsed" Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="🧠 기억 검색 결과" FontWeight="Medium" FontSize="12"
                                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                                       Margin="4,0,0,8"/>
                                            <ListBox x:Name="SearchMemoryResultsList"
                                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                     ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                                     Background="Transparent" BorderThickness="0">
                                                <ListBox.ItemContainerStyle>
                                                    <Style TargetType="ListBoxItem">
                                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="Margin" Value="0"/>
                                                    </Style>
                                                </ListBox.ItemContainerStyle>
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <!-- claude-mem 스타일 검색 결과 카드 -->
                                                        <Border Margin="0,3" CornerRadius="6"
                                                                Background="{DynamicResource CardBrush}">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="4"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                </Grid.ColumnDefinitions>
                                                                <Border Grid.Column="0"
                                                                        Background="{Binding TypeBorderBrush}"
                                                                        CornerRadius="6,0,0,6"/>
                                                                <Border Grid.Column="1" Padding="10,8"
                                                                        Background="{DynamicResource CardBrush}"
                                                                        CornerRadius="0,6,6,0">
                                                                    <StackPanel>
                                                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                                            <TextBlock Text="{Binding TypeIcon}" FontSize="12"
                                                                                       VerticalAlignment="Center" Margin="0,0,6,0"/>
                                                                            <Border Background="{Binding TypeBorderBrush}"
                                                                                    CornerRadius="3" Padding="5,1">
                                                                                <TextBlock Text="{Binding TypeName}"
                                                                                           FontSize="9" Foreground="White"/>
                                                                            </Border>
                                                                        </StackPanel>
                                                                        <TextBlock Text="{Binding Content}"
                                                                                   TextWrapping="Wrap" FontSize="11"
                                                                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                                    </StackPanel>
                                                                </Border>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>
                                        </StackPanel>
                                    </Border>

                                    <!-- 검색 결과: 대화 -->
                                    <Border x:Name="SearchConversationResultsBorder"
                                            Visibility="Collapsed">
                                        <StackPanel>
                                            <TextBlock Text="💬 대화 검색 결과" FontWeight="Medium" FontSize="12"
                                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                                       Margin="4,0,0,8"/>
                                            <ListBox x:Name="SearchConversationResultsList"
                                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                     ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                                     SelectionChanged="SearchConversationResultsList_SelectionChanged"
                                                     Background="Transparent" BorderThickness="0">
                                                <ListBox.ItemContainerStyle>
                                                    <Style TargetType="ListBoxItem">
                                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="Margin" Value="0"/>
                                                    </Style>
                                                </ListBox.ItemContainerStyle>
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <!-- claude-mem 스타일 대화 검색 결과 카드 -->
                                                        <Border Margin="0,3" CornerRadius="6"
                                                                Background="{DynamicResource CardBrush}">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="4"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                </Grid.ColumnDefinitions>
                                                                <Border Grid.Column="0"
                                                                        Background="{StaticResource ConversationBrush}"
                                                                        CornerRadius="6,0,0,6"/>
                                                                <Border Grid.Column="1" Padding="10,8"
                                                                        Background="{DynamicResource CardBrush}"
                                                                        CornerRadius="0,6,6,0">
                                                                    <StackPanel>
                                                                        <Grid Margin="0,0,0,4">
                                                                            <Grid.ColumnDefinitions>
                                                                                <ColumnDefinition Width="Auto"/>
                                                                                <ColumnDefinition Width="*"/>
                                                                                <ColumnDefinition Width="Auto"/>
                                                                            </Grid.ColumnDefinitions>
                                                                            <TextBlock Grid.Column="0"
                                                                                       Text="{Binding DateDisplay}"
                                                                                       FontWeight="SemiBold" FontSize="11"
                                                                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                                            <TextBlock Grid.Column="2"
                                                                                       Text="{Binding LineNumber, StringFormat='줄 {0}'}"
                                                                                       FontSize="9"
                                                                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                                        </Grid>
                                                                        <TextBlock Text="{Binding MatchedLine}"
                                                                                   FontSize="10"
                                                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                                                   TextTrimming="CharacterEllipsis"/>
                                                                    </StackPanel>
                                                                </Border>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>
                                        </StackPanel>
                                    </Border>

                                    <!-- 검색 안내 메시지 -->
                                    <TextBlock x:Name="SearchPlaceholderText"
                                               Text="키워드를 입력하여 기억과 대화를 검색하세요"
                                               HorizontalAlignment="Center"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="12" Margin="0,40,0,0"/>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </Grid>

                    <!-- 하단 컨트롤 -->
                    <Border Grid.Row="2"
                            Background="{DynamicResource CardBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="0,1,0,0"
                            Padding="12,6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock x:Name="MemoryStatsText" Text="총 0개의 기억"
                                       VerticalAlignment="Center" FontSize="11"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>

                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Style="{StaticResource MaterialDesignFlatButton}"
                                        Click="AddMemoryButton_Click"
                                        ToolTip="새 기억 추가" Height="28">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Plus" Width="14" Height="14" VerticalAlignment="Center"/>
                                        <TextBlock Text="추가" Margin="4,0,0,0" FontSize="11"/>
                                    </StackPanel>
                                </Button>

                                <Button Style="{StaticResource MaterialDesignFlatButton}"
                                        Click="ExportMemoryButton_Click"
                                        Margin="4,0,0,0"
                                        ToolTip="MEMORY.md 파일 열기" Height="28" Width="28">
                                    <materialDesign:PackIcon Kind="FileDocumentEdit" Width="14" Height="14"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Orchestrator 컨텐츠 -->
                <ScrollViewer x:Name="OrchestratorContent" Visibility="Collapsed"
                              VerticalScrollBarVisibility="Auto"
                              PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                    <Grid Margin="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- AI Provider 상태 -->
                        <Border Grid.Row="0" Background="{DynamicResource CardBrush}"
                                CornerRadius="6" Padding="12" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="AI Providers" FontWeight="SemiBold" FontSize="12"
                                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock x:Name="OrchestratorModeText"
                                               Text="감지 중..."
                                               FontSize="11"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Margin="0,4,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Border x:Name="ClaudeStatusBadge" CornerRadius="4" Padding="6,2" Margin="4,0"
                                            Background="{DynamicResource BorderBrush}">
                                        <TextBlock Text="Claude" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </Border>
                                    <Border x:Name="CodexStatusBadge" CornerRadius="4" Padding="6,2" Margin="4,0"
                                            Background="{DynamicResource BorderBrush}">
                                        <TextBlock Text="Codex" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </Border>
                                    <Border x:Name="GeminiStatusBadge" CornerRadius="4" Padding="6,2" Margin="4,0"
                                            Background="{DynamicResource BorderBrush}">
                                        <TextBlock Text="Gemini" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- 진행 상황 -->
                        <Border Grid.Row="1" Background="{DynamicResource CardBrush}"
                                CornerRadius="6" Padding="12" Margin="0,0,0,12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="진행 상황" FontWeight="SemiBold" FontSize="12"
                                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock x:Name="OrchestratorProgressText" Grid.Column="1"
                                               Text="0%" FontSize="12"
                                               Foreground="{DynamicResource PrimaryBrush}"/>
                                </Grid>

                                <ProgressBar x:Name="OrchestratorProgressBar"
                                             Grid.Row="1" Value="0" Maximum="100" Height="8"
                                             Margin="0,8,0,0"
                                             Foreground="{DynamicResource PrimaryBrush}"
                                             Background="{DynamicResource BorderBrush}"/>

                                <Grid Grid.Row="2" Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="OrchestratorPendingText" Grid.Column="0"
                                               Text="대기: 0" FontSize="10" HorizontalAlignment="Center"
                                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <TextBlock x:Name="OrchestratorInProgressText" Grid.Column="1"
                                               Text="진행: 0" FontSize="10" HorizontalAlignment="Center"
                                               Foreground="{DynamicResource WarningBrush}"/>
                                    <TextBlock x:Name="OrchestratorCompletedText" Grid.Column="2"
                                               Text="완료: 0" FontSize="10" HorizontalAlignment="Center"
                                               Foreground="{DynamicResource SuccessBrush}"/>
                                    <TextBlock x:Name="OrchestratorFailedText" Grid.Column="3"
                                               Text="실패: 0" FontSize="10" HorizontalAlignment="Center"
                                               Foreground="{DynamicResource ErrorBrush}"/>
                                </Grid>
                            </Grid>
                        </Border>

                        <!-- 플랜 파일 선택 -->
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="플랜 파일"
                                       FontWeight="SemiBold" FontSize="12"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       VerticalAlignment="Center"/>

                            <Button Grid.Column="1"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Click="BrowsePlanFileButton_Click"
                                    ToolTip="플랜 파일 직접 선택 (프로젝트 폴더 내)">
                                <materialDesign:PackIcon Kind="FolderOpen" Width="16" Height="16"/>
                            </Button>

                            <Button Grid.Column="2"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Click="OrchestratorLoadPlanButton_Click"
                                    ToolTip="플랜 파일 새로고침">
                                <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16"/>
                            </Button>
                        </Grid>

                        <!-- 플랜 파일 목록 -->
                        <Border Grid.Row="3" Background="{DynamicResource CardBrush}"
                                CornerRadius="6" Padding="8" Margin="0,6,0,0"
                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" MaxHeight="150"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- 플랜 파일 리스트 -->
                                <ListBox x:Name="PlanFileList" Grid.Row="0"
                                         MaxHeight="120"
                                         SelectionChanged="PlanFileList_SelectionChanged"
                                         Background="Transparent" BorderThickness="0">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Setter Property="Padding" Value="4,2"/>
                                            <Setter Property="Margin" Value="0,1"/>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource BackgroundBrush}"
                                                    CornerRadius="4" Padding="8,6">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="📄" Margin="0,0,8,0"
                                                               VerticalAlignment="Center"/>
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding FileName}"
                                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                                   FontSize="12"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                    </StackPanel>
                                                    <TextBlock Grid.Column="2" Text="{Binding ModifiedTime}"
                                                               FontSize="10"
                                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                                               VerticalAlignment="Center"
                                                               Margin="8,0"/>
                                                    <!-- 삭제 버튼 -->
                                                    <Button Grid.Column="3"
                                                            Style="{StaticResource MaterialDesignIconForegroundButton}"
                                                            Width="22" Height="22"
                                                            Click="RemovePlanFile_Click"
                                                            Tag="{Binding}"
                                                            ToolTip="목록에서 제거">
                                                        <materialDesign:PackIcon Kind="Close" Width="14" Height="14"/>
                                                    </Button>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>

                                <!-- 플랜 파일 없음 -->
                                <TextBlock x:Name="NoPlanFilesText"
                                           Grid.Row="0"
                                           Text="플랜 파일 없음 (/gepetto로 계획 생성)"
                                           Visibility="Collapsed"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           FontSize="11" Margin="0,16"/>

                                <!-- 플랜 미리보기 (마크다운 렌더링) -->
                                <Expander Grid.Row="1" Header="플랜 미리보기" Margin="0,8,0,0"
                                          IsExpanded="False"
                                          Foreground="{DynamicResource TextPrimaryBrush}">
                                    <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto"
                                                  PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                                        <RichTextBox x:Name="PlanPreviewRichText"
                                                     IsReadOnly="True"
                                                     BorderThickness="0"
                                                     Background="Transparent"
                                                     FontFamily="Segoe UI"
                                                     FontSize="12"
                                                     Foreground="{DynamicResource TextPrimaryBrush}"
                                                     Margin="0,8,0,0"
                                                     IsDocumentEnabled="True"/>
                                    </ScrollViewer>
                                </Expander>

                                <!-- 선택된 파일 정보 -->
                                <Border Grid.Row="2" Margin="0,8,0,0"
                                        Background="{DynamicResource BackgroundBrush}"
                                        CornerRadius="4" Padding="8,6"
                                        Visibility="{Binding ElementName=PlanFileList, Path=SelectedItem, Converter={StaticResource BoolToVis}}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="SelectedPlanFileText"
                                                   Text="선택된 파일 없음"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <Button Grid.Column="1"
                                                Style="{StaticResource MaterialDesignFlatButton}"
                                                Click="OpenPlanFileInViewer_Click"
                                                ToolTip="파일 뷰어에서 열기"
                                                Height="24" Width="24">
                                            <materialDesign:PackIcon Kind="OpenInNew" Width="14" Height="14"/>
                                        </Button>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>

                        <!-- 버튼 영역 -->
                        <Grid Grid.Row="4" Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Button x:Name="OrchestratorRefreshButton"
                                    Grid.Column="0"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    HorizontalAlignment="Left"
                                    Click="OrchestratorRefreshButton_Click"
                                    ToolTip="상태 새로고침">
                                <materialDesign:PackIcon Kind="Refresh"/>
                            </Button>

                            <Button x:Name="OrchestratorUpdateButton"
                                    Grid.Column="1"
                                    HorizontalAlignment="Left"
                                    Click="OrchestratorUpdateButton_Click"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    ToolTip="GitHub에서 최신 버전 업데이트"
                                    Margin="8,0,0,0">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="CloudDownload" VerticalAlignment="Center"/>
                                    <TextBlock Text="업데이트" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="11"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="OrchestratorViewTasksButton"
                                    Grid.Column="2"
                                    Content="태스크 보기"
                                    Click="OrchestratorViewTasksButton_Click"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Padding="12,6"
                                    Margin="0,0,8,0"/>

                            <Button x:Name="OrchestratorStartButton"
                                    Grid.Column="3"
                                    Content="PM 시작"
                                    Click="OrchestratorStartButton_Click"
                                    Background="{DynamicResource SuccessBrush}"
                                    Foreground="White"
                                    Padding="16,6"
                                    Margin="0,0,8,0"/>

                            <Button x:Name="OrchestratorResetButton"
                                    Grid.Column="4"
                                    Content="리셋"
                                    Click="OrchestratorResetButton_Click"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Padding="12,6"/>
                        </Grid>
                    </Grid>
                </ScrollViewer>

                <!-- Skills 컨텐츠 -->
                <Grid x:Name="SkillsContent" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 스크롤 영역 -->
                    <ScrollViewer Grid.Row="0"
                                  VerticalScrollBarVisibility="Auto"
                                  PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                        <Grid Margin="12,12,12,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                        <!-- 프로젝트 분석 상태 -->
                        <Border Grid.Row="0"
                                Background="{DynamicResource CardBrush}"
                                CornerRadius="6"
                                Padding="12"
                                Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock x:Name="SkillsProjectNameText"
                                               Text="프로젝트: (분석 대기)"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock x:Name="SkillsStackText"
                                               Text="감지된 기술: -"
                                               FontSize="11"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Margin="0,4,0,0"/>
                                </StackPanel>

                                <Button x:Name="SkillsAnalyzeButton"
                                        Grid.Column="1"
                                        Style="{StaticResource MaterialDesignFlatButton}"
                                        Click="SkillsAnalyzeButton_Click"
                                        ToolTip="프로젝트 기술 스택 분석 및 스킬 추천">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Magnify" VerticalAlignment="Center"/>
                                        <TextBlock Text="분석" Margin="4,0,0,0"/>
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </Border>

                        <!-- 로딩 인디케이터 -->
                        <ProgressBar x:Name="SkillsProgressBar"
                                     Grid.Row="0"
                                     Height="3"
                                     Margin="0,8,0,0"
                                     IsIndeterminate="True"
                                     Visibility="Collapsed"
                                     Foreground="{DynamicResource PrimaryBrush}"/>

                        <!-- 리소스 타입 필터 -->
                        <StackPanel Grid.Row="1" Margin="0,0,0,4">
                            <!-- 첫째 줄: 타입 필터 -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                <RadioButton x:Name="SkillsFilterAll"
                                             Content="전체"
                                             IsChecked="True"
                                             Checked="SkillsFilter_Checked"
                                             Tag="All"
                                             Margin="0,0,8,0"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <RadioButton x:Name="SkillsFilterSkills"
                                             Content="스킬"
                                             Checked="SkillsFilter_Checked"
                                             Tag="Skill"
                                             Margin="0,0,8,0"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <RadioButton x:Name="SkillsFilterAgents"
                                             Content="에이전트"
                                             Checked="SkillsFilter_Checked"
                                             Tag="Agent"
                                             Margin="0,0,8,0"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <RadioButton x:Name="SkillsFilterCommands"
                                             Content="커맨드"
                                             Checked="SkillsFilter_Checked"
                                             Tag="Command"
                                             Margin="0,0,8,0"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <RadioButton x:Name="SkillsFilterHooks"
                                             Content="훅"
                                             Checked="SkillsFilter_Checked"
                                             Tag="Hook"
                                             Margin="0,0,8,0"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <RadioButton x:Name="SkillsFilterMCP"
                                             Content="MCP"
                                             Checked="SkillsFilter_Checked"
                                             Tag="MCP"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                            </StackPanel>
                            <!-- 둘째 줄: 설치 상태 필터 -->
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="설치:" VerticalAlignment="Center" Margin="0,0,8,0"
                                           Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                <RadioButton x:Name="SkillsInstallFilterAll"
                                             Content="모두"
                                             IsChecked="True"
                                             Checked="SkillsInstallFilter_Checked"
                                             Tag="All"
                                             GroupName="InstallFilter"
                                             Margin="0,0,8,0"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <RadioButton x:Name="SkillsInstallFilterInstalled"
                                             Content="설치됨"
                                             Checked="SkillsInstallFilter_Checked"
                                             Tag="Installed"
                                             GroupName="InstallFilter"
                                             Margin="0,0,8,0"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <RadioButton x:Name="SkillsInstallFilterGlobal"
                                             Content="🌐 글로벌"
                                             Checked="SkillsInstallFilter_Checked"
                                             Tag="Global"
                                             GroupName="InstallFilter"
                                             Margin="0,0,8,0"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <RadioButton x:Name="SkillsInstallFilterLocal"
                                             Content="📁 로컬"
                                             Checked="SkillsInstallFilter_Checked"
                                             Tag="Local"
                                             GroupName="InstallFilter"
                                             Margin="0,0,8,0"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <RadioButton x:Name="SkillsInstallFilterNotInstalled"
                                             Content="미설치"
                                             Checked="SkillsInstallFilter_Checked"
                                             Tag="NotInstalled"
                                             GroupName="InstallFilter"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- 추천 리소스 목록 -->
                        <ListBox x:Name="SkillsList" Grid.Row="2"
                                 SelectionMode="Multiple"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                 Background="Transparent"
                                 BorderThickness="0">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="10" Margin="0,3"
                                            Background="{DynamicResource CardBrush}"
                                            CornerRadius="6"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- 체크박스 -->
                                            <CheckBox Grid.Column="0"
                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,8,0"/>

                                            <!-- 정보 -->
                                            <StackPanel Grid.Column="1">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding PriorityIcon}"
                                                               FontSize="12"
                                                               VerticalAlignment="Center"
                                                               Margin="0,0,4,0"/>
                                                    <TextBlock Text="{Binding Name}"
                                                               FontWeight="SemiBold"
                                                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                    <Border Background="{DynamicResource PrimaryBrush}"
                                                            CornerRadius="3"
                                                            Padding="6,1"
                                                            Margin="8,0,0,0"
                                                            Visibility="{Binding ShowInstallLocation, Converter={StaticResource BoolToVis}}">
                                                        <TextBlock Text="{Binding InstallLocation}" FontSize="10" Foreground="White"/>
                                                    </Border>
                                                </StackPanel>
                                                <TextBlock Text="{Binding Description}"
                                                           FontSize="11"
                                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                                           TextWrapping="Wrap"
                                                           Margin="0,2,0,0"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <Border Background="{DynamicResource BackgroundBrush}"
                                                            CornerRadius="3"
                                                            Padding="6,2">
                                                        <TextBlock Text="{Binding TypeName}"
                                                                   FontSize="10"
                                                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                    </Border>
                                                    <Border Background="{DynamicResource BackgroundBrush}"
                                                            CornerRadius="3"
                                                            Padding="6,2"
                                                            Margin="4,0,0,0">
                                                        <TextBlock Text="{Binding Category}"
                                                                   FontSize="10"
                                                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                    </Border>
                                                </StackPanel>
                                            </StackPanel>

                                            <!-- 설치/삭제 버튼 -->
                                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                                <!-- 설치 버튼 (설치 안 된 경우) -->
                                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                                        Width="28" Height="28"
                                                        Click="InstallSingleSkill_Click"
                                                        Tag="{Binding}"
                                                        ToolTip="설치"
                                                        Visibility="{Binding IsNotInstalled, Converter={StaticResource BoolToVis}}">
                                                    <materialDesign:PackIcon Kind="Download" Width="16" Height="16"/>
                                                </Button>
                                                <!-- 삭제 버튼 (설치된 경우) -->
                                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                                        Width="28" Height="28"
                                                        Click="DeleteSingleSkill_Click"
                                                        Tag="{Binding}"
                                                        ToolTip="삭제"
                                                        Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                                                        Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVis}}">
                                                    <materialDesign:PackIcon Kind="DeleteOutline" Width="16" Height="16"/>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        </Grid>
                    </ScrollViewer>

                    <!-- 푸터 (고정) -->
                    <Border Grid.Row="1"
                            Background="{DynamicResource CardBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="0,1,0,0"
                            Padding="12,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock x:Name="SkillsStatsText"
                                       Text="추천: 0개 (설치됨: 0개)"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>

                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Style="{StaticResource MaterialDesignFlatButton}"
                                        Click="SkillsSelectAllButton_Click"
                                        ToolTip="현재 필터에 해당하는 항목 전체 선택">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="CheckAll" VerticalAlignment="Center"/>
                                        <TextBlock Text="전체 선택" Margin="4,0,0,0"/>
                                    </StackPanel>
                                </Button>

                                <Button x:Name="SkillsActionButton"
                                        Click="SkillsActionButton_Click"
                                        Padding="12,6"
                                        Margin="8,0,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon x:Name="SkillsActionIcon" Kind="Download" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="SkillsActionText" Text="설치" Margin="4,0,0,0"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>
