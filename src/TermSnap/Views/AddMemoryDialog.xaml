<Window x:Class="TermSnap.Views.AddMemoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="새 기억 추가"
        Width="450" Height="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <TextBlock Grid.Row="0" Text="새 기억 추가"
                   FontSize="20" FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- 타입 선택 (컨텍스트 트리 구조) -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="섹션" FontWeight="Medium" Margin="0,0,0,8"/>
            <ComboBox x:Name="TypeCombo" Width="220" HorizontalAlignment="Left">
                <ComboBoxItem Content="🏗️ 아키텍처 (설계 결정)" Tag="Architecture" IsSelected="True"/>
                <ComboBoxItem Content="🔄 패턴 (작업 워크플로우)" Tag="Pattern"/>
                <ComboBoxItem Content="🔧 도구 (MCP, 외부 도구)" Tag="Tool"/>
                <ComboBoxItem Content="⚠️ 주의사항 (함정, gotchas)" Tag="Gotcha"/>
                <ComboBoxItem Content="🎯 목표 (프로젝트 목표)" Tag="Goal"/>
                <ComboBoxItem Content="📁 메타 (프로젝트 정보)" Tag="Meta"/>
            </ComboBox>
        </StackPanel>

        <!-- 내용 입력 -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="내용" FontWeight="Medium" Margin="0,0,0,8"/>
            <TextBox x:Name="ContentBox"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Height="100"
                     VerticalScrollBarVisibility="Auto"
                     materialDesign:HintAssist.Hint="기억할 내용을 입력하세요..."/>
        </StackPanel>

        <!-- 중요도 -->
        <StackPanel Grid.Row="3" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="중요도" FontWeight="Medium"/>
                <TextBlock x:Name="ImportanceText" Grid.Column="1"
                           Text="50%" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </Grid>
            <Slider x:Name="ImportanceSlider"
                    Minimum="0" Maximum="100" Value="50"
                    TickFrequency="10" IsSnapToTickEnabled="True"
                    ValueChanged="ImportanceSlider_ValueChanged"/>
        </StackPanel>

        <!-- 버튼 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="취소"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="CancelButton_Click"
                    Margin="0,0,8,0"/>
            <Button Content="추가"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
