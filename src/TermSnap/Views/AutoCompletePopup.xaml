<UserControl x:Class="TermSnap.Views.AutoCompletePopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <!-- 로컬 색상 정의 (누락된 리소스 대체) -->
        <SolidColorBrush x:Key="HoverBrush" Color="#3D3D3D"/>
        <SolidColorBrush x:Key="PrimaryLightBrush" Color="#1E3A5F"/>
    </UserControl.Resources>

    <Border Background="{DynamicResource CardBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="4"
            MaxHeight="250"
            Effect="{DynamicResource MaterialDesignShadowDepth2}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 헤더 -->
            <Border Grid.Row="0"
                    Background="{DynamicResource SurfaceBrush}"
                    Padding="10,6"
                    CornerRadius="4,4,0,0"
                    Margin="0,0,0,4">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="LightningBolt"
                                             Width="14" Height="14"
                                             Foreground="{DynamicResource PrimaryBrush}"
                                             VerticalAlignment="Center"
                                             Margin="0,0,6,0"/>
                    <TextBlock Text="{DynamicResource AutoComplete.Title}"
                               FontSize="11"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="CountText"
                               FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="4,0,0,0"/>
                </StackPanel>
            </Border>

            <!-- 제안 목록 -->
            <ListBox x:Name="SuggestionList"
                     Grid.Row="1"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     SelectionChanged="SuggestionList_SelectionChanged"
                     MouseDoubleClick="SuggestionList_MouseDoubleClick"
                     KeyDown="SuggestionList_KeyDown">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="Margin" Value="0,1"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd"
                                            Background="Transparent"
                                            CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource PrimaryLightBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- 사용자 입력 (질문) -->
                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="{Binding UserInput}"
                                       FontSize="12"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding UserInput}"/>

                            <!-- 유사도 -->
                            <TextBlock Grid.Row="0" Grid.Column="1"
                                       Text="{Binding SimilarityText}"
                                       FontSize="10"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0"/>

                            <!-- 생성된 명령어 -->
                            <TextBlock Grid.Row="1" Grid.ColumnSpan="2"
                                       Text="{Binding GeneratedCommand}"
                                       FontFamily="Consolas"
                                       FontSize="11"
                                       Foreground="{DynamicResource PrimaryBrush}"
                                       TextTrimming="CharacterEllipsis"
                                       Margin="0,2,0,0"
                                       ToolTip="{Binding GeneratedCommand}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- 로딩 표시 -->
            <StackPanel x:Name="LoadingPanel"
                        Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="Collapsed">
                <ProgressBar IsIndeterminate="True"
                             Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Width="16" Height="16"
                             Margin="0,0,8,0"/>
                <TextBlock Text="{DynamicResource AutoComplete.Searching}"
                           FontSize="12"
                           Foreground="{DynamicResource TextSecondaryBrush}"/>
            </StackPanel>

            <!-- 결과 없음 -->
            <TextBlock x:Name="NoResultsText"
                       Grid.Row="1"
                       Text="{DynamicResource AutoComplete.NoResults}"
                       FontSize="12"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Visibility="Collapsed"/>
        </Grid>
    </Border>
</UserControl>
