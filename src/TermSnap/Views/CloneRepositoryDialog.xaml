<Window x:Class="TermSnap.Views.CloneRepositoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{DynamicResource CloneRepo.Title}"
        Height="280" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 헤더 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <materialDesign:PackIcon Kind="Git" Width="24" Height="24" Foreground="#F14E32" VerticalAlignment="Center"/>
            <TextBlock Text="{DynamicResource CloneRepo.Header}" FontSize="18" Foreground="White" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 저장소 URL -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="{DynamicResource CloneRepo.RepositoryURL}" Foreground="#AAAAAA" FontSize="12" Margin="0,0,0,6"/>
            <TextBox x:Name="RepositoryUrlBox"
                     FontSize="13"
                     Padding="10,8"
                     Background="#2D2D30"
                     Foreground="White"
                     BorderBrush="#3E3E42"
                     materialDesign:HintAssist.Hint="{DynamicResource CloneRepo.URLHint}"/>
        </StackPanel>

        <!-- 대상 폴더 -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="{DynamicResource CloneRepo.TargetFolder}" Foreground="#AAAAAA" FontSize="12" Margin="0,0,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TargetPathBox"
                         Grid.Column="0"
                         FontSize="13"
                         Padding="10,8"
                         Background="#2D2D30"
                         Foreground="White"
                         BorderBrush="#3E3E42"
                         materialDesign:HintAssist.Hint="{DynamicResource CloneRepo.TargetHint}"/>
                <Button Grid.Column="1"
                        Content="{DynamicResource CloneRepo.Browse}"
                        Margin="8,0,0,0"
                        Padding="12,8"
                        Click="BrowseButton_Click"/>
            </Grid>
        </StackPanel>

        <!-- 팁 -->
        <Border Grid.Row="3" Background="#252526" CornerRadius="4" Padding="12" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="Information" Width="16" Height="16" Foreground="#4FC3F7" VerticalAlignment="Top" Margin="0,2,8,0"/>
                <TextBlock Text="{DynamicResource CloneRepo.Tip}"
                           Foreground="#808080" FontSize="12" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- 버튼들 -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource CloneRepo.Cancel}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Foreground="#808080"
                    Margin="0,0,8,0"
                    Click="CancelButton_Click"/>
            <Button x:Name="CloneButton"
                    Content="{DynamicResource CloneRepo.Clone}"
                    Background="#007ACC"
                    Foreground="White"
                    Padding="20,8"
                    Click="CloneButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
