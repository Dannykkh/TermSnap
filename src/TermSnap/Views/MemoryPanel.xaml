<UserControl x:Class="TermSnap.Views.MemoryPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="400"
             Background="{DynamicResource MaterialDesignPaper}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 헤더 -->
        <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Brain" Width="24" Height="24"
                                              VerticalAlignment="Center"
                                              Foreground="White"/>
                    <TextBlock Text="AI 장기기억" FontSize="16" FontWeight="Bold"
                               Foreground="White" Margin="8,0,0,0"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <Button x:Name="CloseButton" Grid.Column="2"
                        Style="{StaticResource MaterialDesignIconButton}"
                        Foreground="White" Click="CloseButton_Click"
                        ToolTip="닫기">
                    <materialDesign:PackIcon Kind="Close"/>
                </Button>
            </Grid>
        </Border>

        <!-- 검색 및 필터 -->
        <Border Grid.Row="1" Padding="12,8" Background="{DynamicResource MaterialDesignCardBackground}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 검색창 -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SearchBox" Grid.Column="0"
                             materialDesign:HintAssist.Hint="기억 검색..."
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,8,0"
                             KeyDown="SearchBox_KeyDown"/>

                    <Button x:Name="SearchButton" Grid.Column="1"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Click="SearchButton_Click"
                            ToolTip="검색">
                        <materialDesign:PackIcon Kind="Magnify"/>
                    </Button>
                </Grid>

                <!-- 타입 필터 -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="타입:" VerticalAlignment="Center" Margin="0,0,8,0"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <ComboBox x:Name="TypeFilterCombo" Width="150"
                              SelectionChanged="TypeFilterCombo_SelectionChanged"
                              Foreground="{DynamicResource TextPrimaryBrush}"
                              Background="{DynamicResource CardBrush}">
                        <ComboBoxItem Content="전체" Tag="" IsSelected="True" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <ComboBoxItem Content="📌 사실" Tag="Fact" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <ComboBoxItem Content="💡 선호도" Tag="Preference" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <ComboBoxItem Content="🔧 기술 스택" Tag="TechStack" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <ComboBoxItem Content="📁 프로젝트" Tag="Project" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <ComboBoxItem Content="⚠️ 지침" Tag="Instruction" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <ComboBoxItem Content="📚 학습된 교훈" Tag="Lesson" Foreground="{DynamicResource TextPrimaryBrush}"/>
                    </ComboBox>

                    <Button x:Name="RefreshButton" Style="{StaticResource MaterialDesignFlatButton}"
                            Margin="8,0,0,0" Click="RefreshButton_Click"
                            ToolTip="새로고침">
                        <materialDesign:PackIcon Kind="Refresh"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 메모리 목록 -->
        <ListBox x:Name="MemoryList" Grid.Row="2"
                 Padding="8"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 SelectionChanged="MemoryList_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="12" Margin="0,4"
                            Background="{DynamicResource MaterialDesignCardBackground}"
                            CornerRadius="8"
                            BorderBrush="{DynamicResource MaterialDesignDivider}"
                            BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- 타입 아이콘 + 내용 -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                <TextBlock Text="{Binding TypeIcon}" FontSize="16"
                                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding Content}"
                                           TextWrapping="Wrap"
                                           FontWeight="Medium"/>
                            </StackPanel>

                            <!-- 삭제 버튼 -->
                            <Button Grid.Row="0" Grid.Column="1"
                                    Style="{StaticResource MaterialDesignIconForegroundButton}"
                                    Width="24" Height="24"
                                    Click="DeleteMemory_Click"
                                    Tag="{Binding Id}"
                                    ToolTip="삭제">
                                <materialDesign:PackIcon Kind="Delete" Width="16" Height="16"/>
                            </Button>

                            <!-- 메타 정보 -->
                            <StackPanel Grid.Row="1" Grid.ColumnSpan="2"
                                        Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock Text="{Binding TypeName}"
                                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                                           FontSize="11"/>
                                <TextBlock Text=" · " Foreground="{DynamicResource MaterialDesignBodyLight}"
                                           FontSize="11"/>
                                <TextBlock Text="{Binding ImportanceText}"
                                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                                           FontSize="11"/>
                                <TextBlock Text=" · " Foreground="{DynamicResource MaterialDesignBodyLight}"
                                           FontSize="11"/>
                                <TextBlock Text="{Binding AccessInfo}"
                                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                                           FontSize="11"/>
                            </StackPanel>

                            <!-- 소스 (접힘) -->
                            <Expander Grid.Row="2" Grid.ColumnSpan="2"
                                      Header="원문 보기" Margin="0,4,0,0"
                                      Visibility="{Binding HasSource, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="{Binding Source}"
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                                           FontSize="11"
                                           Margin="0,4,0,0"/>
                            </Expander>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- 하단 컨트롤 -->
        <Border Grid.Row="3" Padding="12,8"
                Background="{DynamicResource MaterialDesignCardBackground}"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 통계 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock x:Name="StatsText" Text="총 0개의 기억"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                </StackPanel>

                <!-- 버튼들 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="AddButton"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Click="AddButton_Click"
                            ToolTip="새 기억 추가">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center"/>
                            <TextBlock Text="추가" Margin="4,0,0,0"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="ExportButton"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Click="ExportButton_Click"
                            Margin="8,0,0,0"
                            ToolTip="MEMORY.md 파일 열기">
                        <materialDesign:PackIcon Kind="FileDocumentEdit"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
