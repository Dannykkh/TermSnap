<Window x:Class="Nebula.Views.FileEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        Title="파일 편집기"
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 툴바 -->
        <ToolBar Grid.Row="0" Background="#F5F5F5">
            <Button x:Name="SaveButton" Click="SaveButton_Click" ToolTip="저장 (Ctrl+S)" Padding="8,4">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="💾" Margin="0,0,5,0"/>
                    <TextBlock Text="저장"/>
                </StackPanel>
            </Button>
            <Separator/>
            <Button x:Name="UndoButton" Click="UndoButton_Click" ToolTip="실행 취소 (Ctrl+Z)" Padding="8,4">
                <TextBlock Text="↩️ 실행 취소"/>
            </Button>
            <Button x:Name="RedoButton" Click="RedoButton_Click" ToolTip="다시 실행 (Ctrl+Y)" Padding="8,4">
                <TextBlock Text="↪️ 다시 실행"/>
            </Button>
            <Separator/>
            <TextBlock Text="인코딩:" VerticalAlignment="Center" Margin="5,0"/>
            <ComboBox x:Name="EncodingComboBox" Width="100" SelectionChanged="EncodingComboBox_SelectionChanged">
                <ComboBoxItem Content="UTF-8" IsSelected="True"/>
                <ComboBoxItem Content="UTF-8 BOM"/>
                <ComboBoxItem Content="EUC-KR"/>
                <ComboBoxItem Content="ASCII"/>
            </ComboBox>
            <Separator/>
            <CheckBox x:Name="WordWrapCheckBox" Content="자동 줄바꿈" VerticalAlignment="Center"
                      Checked="WordWrapCheckBox_Changed" Unchecked="WordWrapCheckBox_Changed"/>
            <Separator/>
            <CheckBox x:Name="ShowLineNumbersCheckBox" Content="줄 번호" VerticalAlignment="Center"
                      IsChecked="True" Checked="ShowLineNumbersCheckBox_Changed" Unchecked="ShowLineNumbersCheckBox_Changed"/>
        </ToolBar>

        <!-- 파일 경로 -->
        <Border Grid.Row="1" Background="#E8E8E8" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="📄 " VerticalAlignment="Center"/>
                <TextBlock x:Name="FilePathText" Grid.Column="1" Text="" VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                <TextBlock x:Name="ModifiedIndicator" Grid.Column="2" Text=" ● 수정됨"
                           Foreground="Orange" Visibility="Collapsed" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- AvalonEdit 텍스트 에디터 -->
        <avalonedit:TextEditor x:Name="TextEditor" Grid.Row="2"
                               FontFamily="Consolas, D2Coding, Courier New"
                               FontSize="14"
                               ShowLineNumbers="True"
                               HorizontalScrollBarVisibility="Auto"
                               VerticalScrollBarVisibility="Auto"
                               TextChanged="TextEditor_TextChanged"/>

        <!-- 상태바 -->
        <StatusBar Grid.Row="3" Background="#F0F0F0">
            <StatusBarItem>
                <TextBlock x:Name="LineColumnText" Text="줄 1, 열 1"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="FileSizeText" Text=""/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="EncodingText" Text="UTF-8"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="SyntaxText" Text="Plain Text"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
